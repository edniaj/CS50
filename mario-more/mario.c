#include <cs50.h>
#include <stdio.h>

void leftTower(void);
int main(void)
{
    int height;
    do
    {
        height = get_int("Height: ");
    } while (height < 1 || height > 8)
}

void leftTower(int height)
{
    for (int i = height; i > 0; i--)
    {
        for(int j=)
    }
}